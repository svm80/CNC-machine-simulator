def get_user_inputs():
    # Collect inputs from user (could be via command line or a GUI form)
    inputs = {
        "part_dimensions": (100, 50, 25),
        "material_type": "Aluminum",
        "tool_diameter": 10,
    }
    return inputs
